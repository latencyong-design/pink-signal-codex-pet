# Notice

Pink Signal is an unofficial fan-made Codex pet.

The public package intentionally avoids publishing generation prompts,
generation job manifests, raw frame extraction metadata, local absolute paths,
or generated-image source records.

Only the clean install package and sanitized QA contact sheet are included.
