# Security Policy

Pink Signal should not require any secrets, API keys, cloud credentials, or
private local paths.

Do not publish raw generation metadata or private Codex files in issues. If an
install bug depends on a local path, redact the username and project folder
before posting.
